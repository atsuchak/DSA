#include <bits/stdc++.h>
using namespace std;

#define ff first
#define ss second
#define all(x) (x).begin(), (x).end()

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	int arr[] = {1, 2, 3, 4, 5};

	cout << "Address of arr(arr): " << arr << endl;

	cout << "Val of 0th index(*arr): " << *arr << endl;

	return 0;
}